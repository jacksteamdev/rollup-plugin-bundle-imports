{
  "name": "rollup-plugin-bundle-imports",
  "version": "0.2.1",
  "description": "Bundle imports separately and use the result in your code.",
  "repository": "bumble-org/rollup-plugin-bundle-imports",
  "license": "MIT",
  "author": "Jack and Amy Steam <jacksteamdev@gmail.com>",
  "files": [
    "build"
  ],
  "main": "build/bundle-cjs.js",
  "module": "build/bundle-esm.js",
  "scripts": {
    "build": "rollup -c",
    "start": "rollup -c -w",
    "test": "jest",
    "preversion": "npm run build",
    "postversion": "git push && npm publish"
  },
  "jest": {
    "clearMocks": true,
    "modulePathIgnorePatterns": [
      "fixtures/dest"
    ],
    "setupFilesAfterEnv": [
      "./tests/jest.setup.js"
    ],
    "transform": {
      ".(js|jsx|ts|tsx)": "@sucrase/jest-plugin"
    }
  },
  "dependencies": {
    "rollup": "^1.13.1",
    "rollup-plugin-commonjs": "^9.3.4",
    "rollup-plugin-node-resolve": "^4.2.4",
    "rollup-pluginutils": "^2.8.1"
  },
  "devDependencies": {
    "@sucrase/jest-plugin": "^2.0.0",
    "dedupe": "^3.0.1",
    "eslint": "^5.16.0",
    "jest": "^24.8.0",
    "sucrase": "^3.10.1"
  }
}
